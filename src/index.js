const message = require('./prompters/message');
const confirm = require('./prompters/confirm');
const choice = require('./prompters/choice');

module.exports = {
  message,
  confirm,
  choice,
};
